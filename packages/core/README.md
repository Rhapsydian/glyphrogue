# @glyphrogue/core (placeholder)

The runtime engine — ships in every production game built with Glyphrogue.
Rendering, ECS/game loop, input, audio, save/load, procedural map
generation, and the scripting/plugin API surface.

No code yet — this package is scaffolded ahead of the deep-dive planning
sessions in `../../BACKLOG.md`. See `../../DESIGN.md` for the architecture
this package will implement.
